#include <stdio.h>
#include <stdlib.h>
#include <string.h>

// size_t foo_len(const char *s){
//   return strlen(s);
// }

int main(int argc, char const *argv[]) {
  const char *a=NULL;
  int b;
  b=*a;
  // printf("Size of a=%lu\n",foo_len(a) );
  exit(0);
  // return 0;
}
